---
import Layout from './Layout.astro';

interface Props {
  frontmatter: {
    title: string;
    description: string;
    pubDate: string;
  };
}

const { frontmatter } = Astro.props;
---

<Layout title={frontmatter.title}>
  <article class="bg-white min-h-screen">
    <!-- Hero Section -->
    <div class="bg-slate-900 py-16 sm:py-24 text-white">
      <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <div class="flex items-center justify-center space-x-2 text-sm font-medium text-blue-400 mb-6 uppercase tracking-wider">
          <span>Market Analysis</span>
          <span>•</span>
          <time datetime={frontmatter.pubDate}>
            {new Date(frontmatter.pubDate).toLocaleDateString('en-US', {
              year: 'numeric',
              month: 'long',
              day: 'numeric'
            })}
          </time>
        </div>
        
        <h1 class="text-4xl font-extrabold tracking-tight text-white sm:text-6xl mb-6 leading-tight">
          {frontmatter.title}
        </h1>
        
        {frontmatter.description && (
          <p class="text-xl text-slate-300 max-w-2xl mx-auto leading-relaxed">
            {frontmatter.description}
          </p>
        )}
      </div>
    </div>

    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 -mt-8 relative z-10">
      <!-- Main Content -->
      <div class="bg-white rounded-2xl shadow-xl p-8 sm:p-12 mb-16">
        <div class="prose prose-lg prose-slate max-w-none 
          prose-headings:font-bold prose-headings:text-slate-900 
          prose-p:text-slate-700 prose-p:leading-relaxed
          prose-a:text-blue-600 prose-a:no-underline hover:prose-a:underline
          prose-img:rounded-xl prose-img:shadow-lg prose-img:my-8
          prose-blockquote:border-l-4 prose-blockquote:border-blue-500 prose-blockquote:bg-slate-50 prose-blockquote:py-2 prose-blockquote:px-4 prose-blockquote:italic
          prose-li:text-slate-700">
          <slot />
        </div>

        <!-- Analysis Resources Section -->
        <aside class="mt-16 pt-8 border-t border-slate-100">
          <h3 class="text-lg font-bold text-slate-900 mb-6 flex items-center">
            <span class="bg-blue-100 text-blue-600 p-2 rounded-lg mr-3">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10" />
              </svg>
            </span>
            Analysis Resources
          </h3>
          <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
            <a 
              href="/charts/silver_gold_analysis.png" 
              target="_blank"
              class="group block p-4 bg-slate-50 rounded-xl border border-slate-200 hover:border-blue-500 hover:bg-white hover:shadow-md transition-all duration-200"
            >
              <div class="flex items-center mb-2">
                <span class="text-2xl mr-3">📊</span>
                <h4 class="font-bold text-slate-900 group-hover:text-blue-600 transition-colors">Interactive Charts</h4>
              </div>
              <p class="text-sm text-slate-600">Historical silver-gold ratio analysis and price trends.</p>
            </a>
            
            <a 
              href="/charts/executive_summary.html" 
              target="_blank"
              class="group block p-4 bg-slate-50 rounded-xl border border-slate-200 hover:border-blue-500 hover:bg-white hover:shadow-md transition-all duration-200"
            >
              <div class="flex items-center mb-2">
                <span class="text-2xl mr-3">⚡</span>
                <h4 class="font-bold text-slate-900 group-hover:text-blue-600 transition-colors">Quick Summary</h4>
              </div>
              <p class="text-sm text-slate-600">Executive summary with key market indicators.</p>
            </a>
          </div>
        </aside>
      </div>

      <!-- Call to Action -->
      <div class="bg-gradient-to-br from-blue-900 to-slate-900 rounded-2xl p-8 sm:p-12 text-white shadow-2xl text-center mb-16">
        <h3 class="text-3xl font-bold mb-4">Stay Ahead of the Market</h3>
        <p class="text-lg text-blue-100 mb-8 max-w-2xl mx-auto">
          Get the latest analysis on precious metals markets, supply-demand dynamics, and price forecasts delivered straight to your feed.
        </p>
        
        <div class="flex flex-col sm:flex-row justify-center items-center gap-6">
          <a 
            href="/" 
            class="inline-flex items-center px-8 py-3 rounded-full text-base font-bold bg-white text-slate-900 hover:bg-blue-50 transition-colors shadow-lg hover:shadow-xl transform hover:-translate-y-0.5 duration-200"
          >
            ← Back to All Posts
          </a>
          
          <div class="text-sm text-blue-200 font-medium">
            Analysis by Jordi Posthumus
          </div>
        </div>
      </div>
    </div>
  </article>
</Layout>